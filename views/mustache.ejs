<% if(typeof mustacheTemplates !== 'undefined') { %>

<% if(mustacheTemplates.indexOf('clockPositionsFillin') > -1) { %>
  <script id="clockPositionsFillin" type="x-tmpl-mustache">
    <div class="row flush">
      {{#positions}}
      <div class="6u position"><a href="#" onclick="CozyHR.pageHelper.instance.clockIn('{{id}}')"><div>{{name}}</div></a></div>
      {{/positions}}
      {{^positions}}
        <div class="12u no-positions">There are no clockable positions here.</div>
      {{/positions}}
    </div>
  </script>
<% } %>

<% if(mustacheTemplates.indexOf('noClockedTime') > -1) { %>
  <script id="noClockedTime" type="x-tmpl-mustache">
    <div class="no-time">
      <div>You do not have any saved time yet.</div>
    </div>
  </script>
<% } %>

<% if(mustacheTemplates.indexOf('clockEntries') > -1) { %>
  <script id="clockEntries" type="x-tmpl-mustache">
    {{#clocks}}
    <div id="clock-{{clockId}}" class="clock-entry">
      <div class="column worked">{{positionName}} <span class="lighter">@ {{locationName}}</span></div>
      <div class="column day">{{{dayName}}}</div>
      <div class="column clockinout">{{clockIn}}</div>
      <div class="column clockinout">{{clockOut}}</div>
      <div class="column total">{{totalTime}}</div>
    </div>
    {{/clocks}}
  </script>
<% } %>

<% if(mustacheTemplates.indexOf('feedItem') > -1) { %>
  <script id="feedItem" type="x-tmpl-mustache">
    {{#feedData}}
    <div id="fid-{{feedId}}" class="feeditem">
      {{{pictureHtml}}}
      <div class="action">{{{contentHtml}}}</div>
      <div class="date">{{date}} ({{officeName}})</div>
      <div class="comments">
        {{#showMoreComments}}
        <div class="manyComments"><a href="#" onclick="CozyHR.pageHelper.instance.loadMoreComments('{{feedId}}')">Show more of the previous <strong><span class="commentsHidden">{{commentsHidden}}</span></strong> comments.</a></div>
        {{/showMoreComments}}
        {{{commentsHtml}}}
      </div>
      <div class="comment"><div class="picture small" style="background-image:url({{pictureUrl}})"></div><input class="writeComment" type="text" data-feedid="{{feedId}}" placeholder="Write a comment.."></div>
    </div>
    {{/feedData}}
  </script>
<% } %>

<% if(mustacheTemplates.indexOf('feedItemComment') > -1) { %>
  <script id="feedItemComment" type="x-tmpl-mustache">
    <div id="cid-{{commentId}}" data-timestamp="{{timestamp}}" style="{{{extraStyling}}}" class="specificComment">
      {{{pictureHtml}}}
      <div class="commentText">{{{contentHtml}}}</div>
      <span class="commentTime">Just now</span>
      <span class="commentLinks">
        {{#isAuthor}}
        - <a href="#" onclick="CozyHR.pageHelper.instance.doDeleteComment(this, '{{commentId}}')">Delete Comment</a>
        {{/isAuthor}}
      </span>
    </div>
  </script>
<% } %>

<% if(mustacheTemplates.indexOf('workingNow') > -1) { %>
  <script id="workingNow" type="x-tmpl-mustache">
    {{#workers}}
    <div class="worker">
      {{{pictureHtml}}}
      <div class="name">{{fullName}}</div>
      <div class="info">{{clockedPosition}} @ {{clockedLocation}}</div>
    </div>
    {{/workers}}
    {{^workers}}
    <div class="no-workers">No one is working right now.</div>
    {{/workers}}
  </script>
<% } %>

<% if(mustacheTemplates.indexOf('accrualSetting') > -1) { %>
  <script id="accrualSetting" type="x-tmpl-mustache">
    <select id="{{id}}AccrualType">
      <option value="disabled">Disabled</option>
      <option value="disabled">Disabled</option>
    </select>
  </script>
<% } %>

<% } %>